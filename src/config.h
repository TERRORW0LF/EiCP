// This ensures that the file will only be included once
// by throwing out all subsequent inclusions during compilation.
#pragma once

// Collection of all relevant includes in the main programm.
#include <iostream>
#include <glad/glad.h>
#include <GLFW/glfw3.h>
#include <fstream>
#include <sstream>
#include <string>
#include <vector>
